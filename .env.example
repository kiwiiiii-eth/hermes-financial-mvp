# Public example only. Do not commit real tokens.
HERMES_API_TOKEN=

# InfluxDB read-only access. When INFLUXDB_READ_TOKEN (or INFLUXDB_TOKEN) is
# unset, the API serves built-in sample data (mode=sample).
INFLUXDB_URL=http://localhost:8086
INFLUXDB_ORG=crypto
INFLUXDB_READ_TOKEN=
